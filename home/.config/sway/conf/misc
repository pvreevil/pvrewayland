
output HDMI-A-1 mode 1920x1080@60Hz

default_border pixel 3
default_floating_border pixel 3

gaps inner 3

exec swayidle -w \
	timeout 600 sglitchlock \
	timeout 720 'loginctl suspend' \
	before-sleep 'sglitchlock' \
